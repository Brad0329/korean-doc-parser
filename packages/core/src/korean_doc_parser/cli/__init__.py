"""CLI entry points for korean-doc-parser.

v0.4 adds ``kdp-label`` (image labelling via Claude Vision). The CLI is
opt-in — only available when the ``[vision]`` extras pull in ``anthropic``.
See ``worklog/011`` for the full Vision decision matrix.
"""
